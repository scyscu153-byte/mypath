# CareerBridge MJC — 실제 테스트 기반 개선 요청

- 저장소: `https://github.com/scyscu153-byte/mypath`
- 목적: 목표 기업/직군 → 요구 역량 → 개인 Gap → 명지전문대 공식 프로그램 추천 흐름의 **신뢰성·최신성·시연 안정성**을 개선한다.

---

## 역할 분담 (2026-08-06 22시경 추가)

파일 경계는 기존과 동일하게: **파이프라인/서버 = 신찬영, UI/앱 = 유초윤**.
데이터 계약(`js/types.js`의 `ProgramMatch` 확장 필드)을 먼저 고정해서, UI는 mock 값으로 먼저 만들고 나중에 실제 값으로 교체한다 (기존 Phase 0 방식과 동일).

### 신찬영 — 백엔드
- **P0-1 서버 검증**: `api/verify-source.js` 새 서버리스 함수. URL fetch(redirect follow) → HTTP 상태 → 최종 URL 도메인 재확인 → 제목 키워드 매칭 → `verified`/`unverified`/`broken` 반환. `js/pipeline.js`의 `searchPrograms()` 결과에 병렬로 적용해 `sourceStatus`/`sourceCheckedAt`/`sourceError` 채우기.
- **P0-2 판정 로직**: `canRecommend(program, today)` 함수 — `availability`(open/upcoming/ongoing/unknown/closed) 판정, 종료 키워드 필터, 날짜 필드 확장. `searchPrograms()`와 `findFallback()` 양쪽 결과에 공통 적용.
- 두 작업 모두 **기존 `matches` 배열에 필드를 추가하는 것**이지 새 데이터 구조가 아니다. 아래 계약(`ProgramMatch` 확장)의 필드명을 그대로 채워서 반환하면 된다.

### 유초윤 — 프론트엔드 (제가 진행 중)
- **P0-1 UI**: 카드에 출처 상태 배지(✅/⚠️/❌), "링크 복사" · "학교 사이트에서 재검색" 보조 버튼
- **P0-2 UI**: 모집상태 배지(🟢🔵🟡⚪), 결과 부족 시 대안 안내 + 필터 옵션(P1-2)
- **P0-3 전체**: 프로필 수정 폼 — `app.js`의 중복된 `loadProfile`/`saveProfile`을 제거하고 `js/profile.js`로 통합, 수정 UI 신설

### 데이터 계약 — `ProgramMatch` 확장 (js/types.js)

```js
{
  // 기존 필드는 그대로
  sourceStatus: "verified" | "unverified" | "broken" | null,  // null = 아직 검증 전(P0-1 미적용 상태와 호환)
  sourceCheckedAt: "2026-08-06T..." | null,
  sourceError: string | null,

  availability: "open" | "upcoming" | "ongoing" | "unknown" | "closed",
  dateConfidence: "verified" | "estimated" | "unknown",
  applicationStartAt: "2026-08-05" | null,
  applicationEndAt: "2026-08-20" | null,
  eventStartAt: "2026-08-25" | null,
  eventEndAt: "2026-08-30" | null,
}
```

UI는 이 필드들이 `null`/`undefined`여도 깨지지 않게 만든다 (검증 전 상태 = 배지 없이 기존처럼 표시). 백엔드가 준비되는 대로 필드가 채워지면 배지가 자동으로 나타나는 방식으로 간다.

---

## v2 검토 결과 (Gemini/ChatGPT 피드백, 2026-08-06 23시경)

P0-1·P0-2·P0-3이 전부 구현·검증된 뒤, 외부 AI(제미나이·챗지피티)가 준 개선 제안서를 받았다.
**전부 그대로 적용하지 않았다** — 이미 실측으로 검증하고 동작 중인 부분과 정면으로 충돌하는 제안이 섞여 있었기 때문이다. 아래는 채택/반려 근거다.

### ✅ 채택 — 낮은 위험, 기존 설계와 충돌 없음

1. **4개월 하드컷오프** (`js/pipeline.js` `judgeAvailability`)
   - 게시일 기준 "오래됨" 판정을 365일 → 120일로 좁혔다.
   - ★단, 이 분기는 `applicationEndAt`/`eventEndAt` 등 명시적 날짜가 하나도 없을 때만 탄다. 지금도 열려 있는 상시 프로그램이 게시만 오래됐다는 이유로 잘못 제외되지 않는다 — 명시적 날짜와 `ONGOING_KEYWORDS`가 우선한다.

2. **평가 코멘트 어조 완화** (`js/pipeline.js` `reviewByPersonas`) — **점수 로직은 그대로 두고 문장 톤만** 수정
   - "부족하다/미흡하다" 대신 프로그램 참여로 얻을 수 있는 경험 중심으로 서술하도록 지시 추가.
   - 점수 자체를 후하게 주라는 지시는 **넣지 않았다** (아래 반려 사유 참조).

### ❌ 반려/보류 — 기존 설계·검증된 동작과 충돌

1. **4개 페르소나를 "항상 높은 점수" 방향으로 조정하라는 제안**
   - 원 제안: "완벽히 부합하지 않더라도 긍정적인 평가(높은 점수)를 부여해 참여 허들을 낮추세요."
   - **반려 이유**: 지금 시스템은 의도적으로 "솔직하게 평가해라, 낮은 점수도 정상이다, 모두에게 높은 점수를 주면 평가의 의미가 없다"를 프롬프트에 박아뒀다. 4개 회사 AI가 실제로 다른 점수를 내야 `disagreement`(의견 갈림) 배지가 의미를 갖고, 그게 "왜 4개 모델을 쓰는가"에 대한 화면상의 유일한 증거다. 전부 후하게 점수를 주면 의견이 갈릴 이유가 없어지고, 지금까지 만든 핵심 차별화 포인트 하나가 사라진다.
   - **대신 채택한 것**: 점수는 그대로 정직하게, **코멘트 문장의 어조만** 부드럽게 (위 채택 2번).

2. **출처를 5개 특정 URL(menu_idx 번호 포함)로 좁히는 화이트리스트**
   - 원 제안: `menu_idx=2711`, `menu_idx=2617`, `menu_idx=208` 등 구체적 번호를 포함한 5개 URL만 허용.
   - **반려 이유**:
     (1) 이 menu_idx 값들이 실제로 맞는지 **검증되지 않았다.** 링크 하나하나를 실제로 열어 확인한 적이 없는 값을 그대로 코드에 박으면, 우리가 지금까지 "출처 없으면 안 보여준다"는 원칙으로 싸워온 바로 그 문제(그럴듯하지만 확인 안 된 URL)를 스스로 만드는 꼴이다.
     (2) 이미 8개 서브도메인(www/cls/mpu/sanhak/rise/inter/mrcc/edu)에서 검색하도록 실측 기반으로 만들어놨고, 특히 "www에만 몰리는 문제"를 발견해서 `mpu.mjc.ac.kr`·`sanhak.mjc.ac.kr`를 강제로 확인하게 만드는 프롬프트까지 이미 검증 완료했다. 5개 URL로 좁히면 이 작업이 전부 무효화되고 sanhak(현장실습), cls(학과공지) 등 이미 잘 나오던 출처가 사라진다.
   - **대신 유지**: 기존 8개 서브도메인 + `isAllowedSource()`(mjc.ac.kr 도메인 검증) + P0-1 실제 링크 검증(verify-source) 조합. 이게 이미 "화이트리스트"의 목적(신뢰 가능한 출처만)을 달성하면서, 검증되지 않은 특정 URL을 하드코딩하는 리스크가 없다.

3. **점수 체계 전면 재설계** (목표연결도 35% · 성장도움도 35% · 참여추천도 20% · 공식정보확인도 10%, 4단계 등급)
   - **반려 이유**: 지금 구조는 "4개 회사 AI가 각자 다른 렌즈(실무/학업연계/비용시간/시장트렌드)로 독립 평가 → 의견 갈림을 그대로 보여줌"이다. 제안된 구조는 "하나의 종합 점수"로 재설계하는 것이라 `reviewByPersonas`·`disagreement` 계산·카드 UI를 전부 다시 짜야 한다. 마감(8/7 12시)이 몇 시간 안 남은 시점에 이 정도 재구조화는 위험이 이득보다 크다.

4. **관심있음 → 신청예정 → 신청완료 → 참여중 → 이행완료 상태 관리(P1-2)**
   - 좋은 기능이지만 새 데이터 구조 + UI가 필요한 범위. 시간상 P1로 보류.

### 정리

> 세 가지 제안 중 **URL 화이트리스트**와 **점수 체계 재설계**는 검증되지 않았거나(1) 이미 테스트를 마친 기존 구현을 무효화하는(2, 3) 리스크가 있어 반려했다. **어조 완화**만 점수 로직을 건드리지 않는 안전한 범위로 골라 반영했다.

---

## 실제 테스트에서 발견한 문제

1. **원문 보기 링크 오류**
   - 링크 클릭 시 `요청하신 자료를 찾을 수 없습니다.` 또는 `사이트에 연결할 수 없습니다.`가 발생한다.
   - 참고: 사용자 안내 문구는 **“요청하신 자료를 찾을 수 없습니다.”**가 자연스럽다.

2. **기간이 지난 프로그램 노출**
   - 이미 신청 또는 진행 기간이 끝난 프로그램이 추천 결과에 계속 표시된다.

3. **내 프로필 수정 불가**
   - 내 프로필은 조회만 가능하고, 학과·학년·기술·자격증 등의 정보를 수정할 수 없다.

---

# P0-1. 원문 보기 링크 오류 수정 및 링크 신뢰성 강화

## 목표

형식만 `mjc.ac.kr`인 URL이 아니라, **지금 실제로 열리는 공식 원문 페이지**만 기본 추천 결과에 표시한다.

## 개선 요구사항

### 1) 추천 데이터에 출처 상태를 명시

```js
{
  sourceUrl: "원문 URL",
  sourceStatus: "verified" | "unverified" | "broken",
  sourceCheckedAt: "2026-08-06T...",
  sourceError: null,
  sourceDomain: "cls.mjc.ac.kr"
}
```

### 2) 서버 측 URL 검증 추가

브라우저에서는 CORS 문제가 발생할 수 있으므로, 가능하면 Vercel Serverless API(예: `/api/verify-source`)에서 검증한다.

검증 항목:

- URL 파싱 가능 여부
- `mjc.ac.kr` 또는 허용된 하위 도메인 여부
- 리다이렉트 후 HTTP 상태 코드가 200~299인지
- 최종 URL도 `mjc.ac.kr` 계열인지
- HTML 제목 또는 본문 일부에 프로그램 제목의 핵심 키워드가 포함되는지

```js
async function verifySourceUrl(url, expectedTitle) {
  // 1. mjc.ac.kr 또는 하위 도메인 확인
  // 2. fetch(url, { redirect: "follow" }) 수행
  // 3. HTTP 200~299 여부 확인
  // 4. 최종 URL의 hostname 재검증
  // 5. HTML title/본문에 expectedTitle 핵심 키워드 포함 여부 확인
  // 6. verified / broken / unverified 반환
}
```

### 3) 출처 상태별 노출 기준

```text
verified   → 기본 추천 목록에 표시
unverified → “원문 확인 필요” 배지와 함께 하단 후보로 표시
broken     → 기본 추천 목록에서 제외
```

### 4) 추천 카드 UI

```text
✅ 공식 원문 확인됨
게시일: YYYY.MM.DD
[공식 공지 열기 ↗]

⚠️ 원문 접근 여부를 확인하지 못했습니다.
프로그램 신청 전 운영 부서 공지를 다시 확인해주세요.
[원문 열기 ↗]

❌ 원문 링크를 확인할 수 없어 추천에서 제외했습니다.
```

### 5) 링크 보조 기능

원문 링크가 실패하는 상황을 대비하여 다음 기능을 제공한다.

```text
[공식 공지 열기 ↗]
[링크 복사]
[학교 사이트에서 제목으로 다시 검색]
```

‘학교 사이트에서 제목으로 다시 검색’은 다음 검색어를 활용해도 된다.

```text
site:mjc.ac.kr "프로그램명"
```

---

# P0-2. 기간이 지난 프로그램 추천 방지

## 목표

현재 신청 가능하거나 모집 예정인 프로그램을 우선 추천하고, 이미 종료된 프로그램은 기본 추천에서 제외한다.

## 개선 원칙

```text
현재 신청 가능 / 모집 예정 / 상시 참여 가능
→ 기본 추천

일정을 확인할 수 없음
→ “현재 모집 여부 확인 필요” 표시 후 보조 후보로 제공

신청 마감 또는 행사 종료
→ 기본 추천에서 제외
```

## 구현 요구사항

### 1) 프로그램 일정 데이터 확장

```js
{
  postedAt: "2026-08-01",
  applicationStartAt: "2026-08-05",
  applicationEndAt: "2026-08-20",
  eventStartAt: "2026-08-25",
  eventEndAt: "2026-08-30",
  availability: "open" | "upcoming" | "ongoing" | "unknown" | "closed",
  dateConfidence: "verified" | "estimated" | "unknown"
}
```

### 2) 추천 노출 기준을 코드로 강제

```js
function canRecommend(program, today = new Date()) {
  // closed: 제외
  // applicationEndAt < today: 제외
  // eventEndAt < today: 제외
  // 2025년 단발성 프로그램: 제외
  // unknown: 보조 후보로 표시
  // open / upcoming / ongoing: 우선 추천
}
```

우선순위:

```text
1순위: 모집 중 (open)
2순위: 모집 예정 (upcoming)
3순위: 상시/진행 중 (ongoing)
4순위: 일정 미확인 (unknown) — 별도 표시
제외: 마감 (closed)
```

### 3) fallback 데이터 분류

기존 fallback 데이터는 다음 3그룹으로 나눈다. (시간상 143건 전체를 수동 재분류하지 않고, `canRecommend()`를 fallback 결과에도 동일하게 적용하는 방식으로 대체 가능)

```text
A. currentPrograms
- 현재 모집 중, 모집 예정, 상시 참여 가능성이 높은 프로그램

B. unknownSchedulePrograms
- 일정 확인이 어려운 프로그램
- “현재 모집 여부는 원문 확인 필요”로만 노출

C. archivePrograms
- 종료, 과거 행사, 결과 공지, 2025년 단발성 프로그램
- 추천 후보에서 제외
```

### 4) 종료 키워드 필터

제목 또는 요약에 아래 키워드가 있으면 종료 후보로 분류한다.

```text
마감
종료
결과 발표
수상작
성과 공유
후기
완료
지난
2025학년도
2024학년도
수료식
시상식
```

> 단, `2026학년도`는 제외 기준이 아니다.

### 5) UI에 일정 상태 표시

```text
🟢 현재 모집 중
🔵 모집 예정
🟡 모집 일정 확인 필요
⚪ 상시 참여 가능 여부 확인 필요
```

카드 예시:

```text
[🟢 현재 모집 중]

2026 AI 비교과 프로그램
신청 기간: 2026.08.07 ~ 2026.08.20
연결 역량: AI 활용, 프로젝트 경험
[공식 공지 열기 ↗]
```

일정이 불확실한 경우:

```text
[🟡 모집 일정 확인 필요]

프로그램명
게시일: 2026.07.25
현재 모집 여부는 공식 공지에서 확인해주세요.
[공식 공지 열기 ↗]
```

---

# P0-3. 내 프로필 수정 기능 추가

## 목표

사용자가 최초 입력한 프로필을 수정하고, 수정 결과가 다음 Gap 분석에 즉시 반영되도록 한다.

## 현재 구조 활용

기존 `profile.js`의 `loadProfile()`, `saveProfile()`, `markCompleted()`, `unmarkCompleted()`를 활용한다. 새로운 저장 구조를 만들기보다, **프로필 편집 UI와 기존 localStorage 저장 로직을 연결**한다.

## 구현 요구사항

### 1) 내 프로필 화면에 수정 버튼 추가

```text
내 프로필
- 학과
- 학년
- 보유 기술
- 자격증
- 이행한 프로그램

[프로필 수정]
[새 목표 분석하기]
```

### 2) 수정 모드 필드

```text
학년: select
나이: number
학과: text
자격증: 쉼표 입력 또는 태그 입력
보유 기술: 쉼표 입력 또는 태그 입력
참여 방식 선호: team / solo / 미선택
```

### 3) 저장·취소·초기화

```text
[저장하기]
[취소]
[프로필 초기화]
```

초기화는 확인 모달을 반드시 표시한다.

```text
정말 프로필과 이행 이력을 모두 삭제할까요?
이 작업은 되돌릴 수 없습니다.

[취소] [삭제]
```

### 4) 저장 후 즉시 반영

```text
프로필 수정
→ localStorage 저장
→ 현재 메모리 상태(profile) 갱신
→ 프로필 화면 재렌더링
→ 다음 Gap 분석에 수정된 정보 반영
```

### 5) 직접 입력 기술과 이행 이력 기술 구분

```text
보유 기술
- Java                  [직접 입력]
- Unity                 [직접 입력]
- Git/GitHub 협업       [프로그램 이수]
```

이행 이력 기반 기술을 삭제하려 할 때는 경고를 표시한다.

```text
이 기술은 프로그램 이행 이력과 연결되어 있습니다.
삭제하면 해당 이행 기록도 함께 변경될 수 있습니다.
```

### 6) 이행 완료 저장 흐름 점검

아래 흐름이 실제로 같은 `mypath.profile` 데이터를 일관되게 쓰는지 확인한다.

```text
프로그램 이행 완료
→ localStorage에 completedActivities 저장
→ 내 프로필 화면에서 즉시 표시
→ 새 목표 분석 시 completedActivities 및 skills가 프롬프트에 반영
→ 새로고침 후에도 유지
```

---

# P1-1. 추천 점수에 현재성 반영

추천 적합도에 일정 최신성을 반영한다.

```text
최종 추천 점수 =
직무 적합성        40%
Gap 해소도         30%
출처 신뢰성        20%
현재성/일정 상태   10%
```

> 단, `closed` 상태는 점수를 낮추는 것이 아니라 추천 결과에서 제외한다.

---

# P1-2. 추천 결과가 없을 때 빈 화면 방지

마감 프로그램을 제거하면 특정 Gap에서 추천 결과가 없을 수 있다. 이때 빈 화면으로 끝내지 않는다.

```text
현재 신청 가능한 공식 프로그램을 찾지 못했습니다.

대신 아래를 확인해보세요.
1. 모집 일정 확인이 필요한 관련 프로그램
2. SMART CARE 비교과 프로그램
3. 산학협력처 / 현장실습지원센터 공지
4. 학교 사이트에서 직접 검색하기
```

필터 옵션:

```text
[현재 모집 중만 보기]
[모집 일정 확인 필요 프로그램도 보기]
[종료 프로그램 포함해서 참고용으로 보기]
```

기본값:

```text
현재 모집 중 + 모집 예정 + 상시 가능성 높은 프로그램
```

---

# P1-3. 실제 배포 환경 테스트 체크리스트

## 링크 테스트

```text
[ ] 추천 카드의 원문 링크 10개를 직접 클릭한다.
[ ] 정상 이동 또는 검증 상태 표시를 확인한다.
[ ] 404 / 연결 불가 URL은 기본 추천 목록에 없는지 확인한다.
[ ] 최종 URL이 mjc.ac.kr 또는 하위 도메인인지 확인한다.
```

## 기간 테스트

```text
[ ] 2025년 단발성 프로그램이 기본 추천에 나오지 않는다.
[ ] 이미 종료된 하계/동계 프로그램이 기본 추천에 나오지 않는다.
[ ] 현재 모집 중 프로그램은 모집 상태와 기간이 표시된다.
[ ] 일정 미확인 프로그램에는 경고 문구가 있다.
```

## 프로필 테스트

```text
[ ] 학과 수정 후 새 분석에 반영된다.
[ ] 보유 기술 수정 후 Gap 분석이 달라진다.
[ ] 자격증 추가/삭제가 저장된다.
[ ] 프로그램 이행 완료 후 내 프로필에 즉시 나타난다.
[ ] 새로고침 후에도 프로필과 이행 이력이 유지된다.
[ ] 초기화 시 확인 모달이 나타난다.
```

---

# 작업 우선순위

```text
P0-1. 원문 링크 실제 접근 검증 + 죽은 링크 추천 제외
P0-2. 마감/종료 프로그램 추천 제외 + 일정 상태 표시
P0-3. 내 프로필 수정 기능 + localStorage 저장 반영

P1-1. 추천 점수에 현재성/일정 상태 반영
P1-2. 추천 결과 부족 시 빈 화면 대신 대안 제시
P1-3. 배포 환경에서 링크·기간·프로필 실제 테스트
```

---

# 최종 완료 기준

```text
[ ] ‘원문 보기’ 링크가 실제 접근 가능한 공식 페이지로 연결된다.
[ ] 접근 불가 또는 깨진 링크는 추천 결과에서 제외된다.
[ ] 마감된 프로그램은 기본 추천 목록에 표시되지 않는다.
[ ] 모든 추천 카드에 일정 상태 또는 ‘일정 확인 필요’ 상태가 표시된다.
[ ] 사용자가 내 프로필의 학과, 학년, 기술, 자격증 등을 수정할 수 있다.
[ ] 수정된 프로필은 새 분석 결과에 반영된다.
[ ] 프로그램 이행 이력은 저장되고 새로고침 후에도 유지된다.
[ ] API 검색 실패 시에도 fallback 데이터는 최신성 필터를 거쳐 제공된다.
```

> **가장 중요한 개선 방향: ‘공식 도메인인지’만 확인하지 말고, ‘지금 실제로 열리는 최신 프로그램 공지인지’까지 확인한 뒤 추천한다.**
